# The root .yarnrc.yml's afterInstall hook (Husky, quasar prepare, icon
# generation, electron-rebuild) is only meaningful for the root project.
# It still gets inherited here since Yarn cascades .yarnrc.yml settings,
# so it must be disabled for this isolated src-electron install.
afterInstall: ''
