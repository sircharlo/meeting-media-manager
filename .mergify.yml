pull_request_rules:
  - name: Automatic merge for crowdin translations
    conditions:
      - and:
          - author=sircharlo
          - title~=^chore. new translations
    actions:
      update:
      merge:
        method: squash
  - name: Automatic merge for trusted authors and dependabot
    conditions:
      - or:
          - author=mtdvlpr
          - and:
              - author=dependabot[bot]
              - or:
                 - title~=^Bump [^\s]+ from ([\d]+)\..+ to \1\.
                 - title~=^chore\(deps-dev\). bump [^\s]+ from ([\d]+)\..+ to \1\.
    actions:
      update:
      merge:
